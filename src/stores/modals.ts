import { writable } from 'svelte/store';

export const statsModalState = writable(false);
export const helpModalState = writable(false);
export const settingsModalState = writable(false);
export const aboutModalState = writable(false);
